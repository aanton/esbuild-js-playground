{
  "name": "esbuild-js-demo",
  "version": "1.0.0",
  "description": "Playground JavaScript project using esbuild",
  "private": "true",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch --server",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.13.12",
    "glob": "^7.2.0",
    "minimist": "^1.2.5",
    "servor": "^4.0.2",
    "standard": "^16.0.4"
  },
  "eslintConfig": {
    "extends": "standard",
    "ignorePatterns": [
      "public/*"
    ]
  }
}
